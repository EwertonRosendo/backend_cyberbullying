<%= form_with(model: case) do |form| %>
  <% if case.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(case.errors.count, "error") %> prohibited this case from being saved:</h2>

      <ul>
        <% case.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.label :institution_answer, style: "display: block" %>
    <%= form.text_area :institution_answer %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
