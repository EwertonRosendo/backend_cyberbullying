<%= form_with(model: school_answer) do |form| %>
  <% if school_answer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(school_answer.errors.count, "error") %> prohibited this school_answer from being saved:</h2>

      <ul>
        <% school_answer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :question_id, style: "display: block" %>
    <%= form.text_field :question_id %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :answer, style: "display: block" %>
    <%= form.text_area :answer %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
